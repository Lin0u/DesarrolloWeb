<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Calificación del pedido</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 40px;
      background: #f9f9f9;
    }

    h2 {
      margin-bottom: 20px;
    }

    .stars {
      display: flex;
      gap: 10px;
      font-size: 40px;
      cursor: pointer;
    }

    .stars span {
      color: #ccc;
      transition: 0.3s;
    }

    .stars span.active {
      color: gold;
    }

    textarea {
      display: block;
      margin-top: 20px;
      width: 100%;
      height: 80px;
      padding: 10px;
      resize: none;
    }

    button {
      margin-top: 15px;
      padding: 10px 20px;
      background-color: #007bff;
      border: none;
      border-radius: 5px;
      color: white;
      font-size: 16px;
      cursor: pointer;
    }

    button:hover {
      background-color: #0056b3;
    }

    .mensaje {
      margin-top: 20px;
      padding: 15px;
      border: 1px solid #ddd;
      background: #e9ffe9;
      color: #333;
      display: none;
    }
  </style>
</head>
<body>

  <h2>Calificación del pedido</h2>
  
  <div class="stars" id="stars">
    <span data-value="1">★</span>
    <span data-value="2">★</span>
    <span data-value="3">★</span>
    <span data-value="4">★</span>
    <span data-value="5">★</span>
  </div>

  <textarea id="comentario" placeholder="Añadir comentarios..."></textarea>

  <button id="enviar">Enviar</button>

  <div class="mensaje" id="mensaje">
    ¡Gracias por su opinión!
  </div>

  <script>
    const stars = document.querySelectorAll("#stars span");
    let puntuacion = 0;

    stars.forEach(star => {
      star.addEventListener("click", () => {
        puntuacion = star.dataset.value;
        stars.forEach(s => s.classList.remove("active"));
        for (let i = 0; i < puntuacion; i++) {
          stars[i].classList.add("active");
        }
      });
    });

    document.getElementById("enviar").addEventListener("click", () => {
      const comentario = document.getElementById("comentario").value;
      
      if (puntuacion === 0) {
        alert("Por favor selecciona una calificación");
        return;
      }

      // Aquí iría el llamado al API en una aplicación real
      console.log("Puntuación:", puntuacion);
      console.log("Comentario:", comentario);

      document.getElementById("mensaje").style.display = "block";
    });
  </script>

</body>
</html>
